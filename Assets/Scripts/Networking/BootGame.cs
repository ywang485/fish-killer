using UnityEngine;

/// boot the game
/// host a game or join a game
public class BootGame : MonoBehaviour {
    void Start () {
        // TODO check if a lobby exists
        // if no, host the game and spawn as the killer
        // if yes, join the game and spawn as a fish
    }
}
